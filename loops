'''def first_n(n):
  Build and return list
  num, nums = 0, []
  while num < 10:
    num.append(num)
    num +=1
    return num
sum_of_first_n = sum (first_n (1000000)) '''

'''for loops
fruits = ["banana", "mango", "apple", "cherry"]
for x in fruits:
  print(x)
  fruits = "banana"
  for x in fruits:
    print(x)'''
fruits = ["banana", "mango", "apple", "cherry"]
for x in fruits:
  if x == "apple":
    break
  print(x)
